name: Maven Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    container: maven:latest
    steps:
      - uses: actions/checkout@v4
      - name: Run tests
        run: mvn test

